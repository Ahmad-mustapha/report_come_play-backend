# Database
DATABASE_URL="postgresql://postgres:report_come_play@12@db.youmipkmbwwsvwwwvgxw.supabase.co:5432/postgres"

# Example with Supabase:
# DATABASE_URL="postgresql://postgres.xxxxxxxxxxxxx:YOUR-PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres"

# Server
PORT=5000
NODE_ENV=development

# JWT Authentication
# Generate a unique secret for each environment (dev, staging, production)
# Run this command to generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# NEVER use the same secret across environments!
JWT_SECRET=your-generated-secret-here-replace-this
JWT_EXPIRES_IN=1h

# CORS (Frontend URL)
ALLOWED_ORIGINS=http://localhost:3000
# Email (Resend)
RESEND_API_KEY=re_123456789
FROM_EMAIL=onboarding@resend.dev
